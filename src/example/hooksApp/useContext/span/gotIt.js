"use strict";

import CloseLinkButton from "../button/link/close";

const GotItSpan = (props) => {
  return (

    <span className="got-it">
      [
      <CloseLinkButton/>
      ]
    </span>

  );
}

export default GotItSpan;
